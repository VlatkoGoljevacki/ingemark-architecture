services:
  # Serve the architecture index locally
  serve:
    image: node:22-alpine
    ports:
      - "3000:3000"
    volumes:
      - .:/site
    working_dir: /site
    command: npx serve -l 3000 .
